services:
  local-codex-kit:
    environment:
      LOCAL_CODEX_TRANSFORMERS_DEVICE: ${LOCAL_CODEX_TRANSFORMERS_DEVICE:-auto}
      LOCAL_CODEX_TRANSFORMERS_OFFLOAD_DIR: ${LOCAL_CODEX_TRANSFORMERS_OFFLOAD_DIR:-/workspace/transformers-offload}
      LOCAL_CODEX_TRANSFORMERS_MIN_OUTPUT_TOKENS: ${LOCAL_CODEX_TRANSFORMERS_MIN_OUTPUT_TOKENS:-64}
